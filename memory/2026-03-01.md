# 2026-03-01

## Claw Pen Progress

- **GUI working** - Tauri desktop app connects to agents
- **Core selection** when creating agents ✓
- **RAM config** ✓
- **Secrets management** ✓
- **No-auth mode fix** - WebSocket connects after 2s timeout if no challenge
- **Settings modal** - ⚙️ button to add API keys securely

### New Features Added Today
1. `Mount` and `NetworkPolicy` types for safe agent isolation (started)
2. `code_index.rs` - Semantic indexing for large codebases (done)
3. `safe-code-assistant` template (in progress)

### Current Issues
- Agent chat fails with 401 - need valid Kimi API key
- Settings button just added - need to test

## System

- WhatsApp gateway brief disconnect at 08:49 (status 428) - auto-recovered in ~5 seconds
- Added port reference to TOOLS.md + `ports` alias to .bashrc
